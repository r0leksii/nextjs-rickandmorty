version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci # or pnpm ci if you have a lockfile
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
