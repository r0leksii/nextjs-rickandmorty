version: 1
frontend:
  phases:
    preBuild:
      commands:
        - curl -f https://get.pnpm.io/v6.16.js | node - add --global pnpm
        - pnpm install
    build:
      commands:
        - pnpm next build
        - pnpm next export
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
